---
name: Rocket Chat Message

on:
  pull_request:
    types: 
      - opened

jobs:
  chatmsg:
    runs-on: ubuntu-latest
    name: PR chat message
    steps:
      - name: Rocket.Chat Notification
        uses: RocketChat/Rocket.Chat.GitHub.Action.Notification@master
        if: always()
        with:
          type: ${{ job.status }}
          job_name: '*Pull Request Message*'
          mention: 'here'
          mention_if: 'failure'
          channel: ${{ secrets.ROCKET_CHAT_CHAN }}
          url: ${{ secrets.ROCKETCHAT_WEBHOOK }}

